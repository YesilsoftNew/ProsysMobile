<?xml version="1.0" encoding="utf-8"?>

<Grid xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:buttons="clr-namespace:ProsysMobile.CustomControls.Button;assembly=ProsysMobile"

             x:Class="ProsysMobile.CustomControls.Bar.CustomPageBarPrimary"
             Padding="16,21"
             HorizontalOptions="FillAndExpand"
             VerticalOptions="FillAndExpand">
    
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"></ColumnDefinition>
        <ColumnDefinition Width="AUTO"></ColumnDefinition>
    </Grid.ColumnDefinitions>
    
    <Label
        x:Name="ItemTitle"
        Grid.Column="0"
        HorizontalOptions="StartAndExpand"
        VerticalOptions="CenterAndExpand"
        Style="{DynamicResource Header7}" />
    
    <buttons:CustomButtonLabelPrimary
        x:Name="ItemButton"
        Grid.Column="1"
        HorizontalOptions="EndAndExpand"
        VerticalOptions="CenterAndExpand"
        Style="{DynamicResource Link2}" />
    
</Grid>