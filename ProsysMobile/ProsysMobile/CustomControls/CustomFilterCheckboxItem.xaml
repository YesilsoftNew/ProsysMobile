<?xml version="1.0" encoding="utf-8" ?>
<Grid xmlns="http://xamarin.com/schemas/2014/forms"
      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
      xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
      x:Class="WiseMobile.CustomControls.CustomFilterCheckboxItem"
      x:Name="ItemGrid" 
      RowSpacing="0"
      VerticalOptions="FillAndExpand" 
      HorizontalOptions="FillAndExpand" 
      BackgroundColor="{DynamicResource TransparentColor}">

    <Grid.GestureRecognizers>
        <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped" />
    </Grid.GestureRecognizers>
    
    <Grid.RowDefinitions>
        <RowDefinition Height="auto"/>
        <RowDefinition Height="auto"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="auto"/>
        <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <CheckBox Grid.Row="0" Grid.Column="0" x:Name="ItemCheckbox" Color="{DynamicResource Primary.Base}" CheckedChanged="ItemCheckbox_CheckedChanged" VerticalOptions="CenterAndExpand" HorizontalOptions="Start" Margin="0,20,0,20"/>
    <Label Grid.Row="0" Grid.Column="1" x:Name="ItemLabel" Style="{DynamicResource Views.Tables.Content}" VerticalOptions="CenterAndExpand" HorizontalOptions="StartAndExpand" />
    <yummy:PancakeView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" x:Name="ItemLine"  Style="{DynamicResource Views.VehicleDetail.Line}"/>
</Grid>