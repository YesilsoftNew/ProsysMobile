<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary
    x:Class="ProsysMobile.Themes.Customs.Backdrop"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:backdrop="clr-namespace:ProsysMobile.Models">

    <Style x:Key="Customs.Backdrop.Backdrop" TargetType="backdrop:CustomBackdrop">
        <Setter Property="BorderColor" Value="{DynamicResource TransparentColor}" />
        <Setter Property="BorderThickness" Value="3" />
        <Setter Property="CornerRadius" Value="-1" />
        <Setter Property="IndicatorColor" Value="#CDCFD0" />
        <Setter Property="RoundedCorners" Value="topleft,topright" />
        <Setter Property="ShadowColor" Value="{DynamicResource TransparentColor}" />
        <Setter Property="HasKeyboardOffset" Value="False" />
        <Setter Property="ShadowOpacity" Value="0" />
    </Style>

    <Style x:Key="Customs.Backdrop.MainGrid" TargetType="Grid">
        <Setter Property="BackgroundColor" Value="{DynamicResource White1}" />
        <Setter Property="VerticalOptions" Value="EndAndExpand" />
        <Setter Property="HorizontalOptions" Value="FillAndExpand" />
        <Setter Property="Padding" Value="0,0,0,60" />
        <Setter Property="RowSpacing" Value="0" />
    </Style>

</ResourceDictionary>